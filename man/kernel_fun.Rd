% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_fun.R
\name{kernel_fun}
\alias{kernel_fun}
\title{Asymmetric/symmetric kernel with interpretable neighbor borrowing strengths}
\usage{
kernel_fun(
  dose,
  dose_set,
  symmetric = FALSE,
  k_left = NULL,
  k_right = NULL,
  ref_gap = NULL
)
}
\arguments{
\item{dose}{Numeric scalar. Target dose value (standardized dose level, not an index).}

\item{dose_set}{Numeric vector. Dose locations at which to evaluate similarity (standardized doses).
Typically strictly increasing.}

\item{symmetric}{Logical. If \code{TRUE}, use a single symmetric decay; if \code{FALSE} (default), use an
asymmetric decay with separate left/right decay rates.}

\item{k_left}{Numeric scalar in \verb{(0,1)}. Desired kernel value at distance \code{ref_gap} for dose locations
strictly below \code{dose} (\code{dose_set < dose}). Default is \code{0.2} when omitted.}

\item{k_right}{Numeric scalar in \verb{(0,1)}. Desired kernel value at distance \code{ref_gap} for dose locations
at or above \code{dose} (\code{dose_set >= dose}). Default is \code{0.8} when omitted.}

\item{ref_gap}{Numeric scalar. Reference distance used to interpret \code{k_left} and \code{k_right}.
If \code{NULL}, defaults to \code{min(diff(dose_set))}. Must be positive and finite.}
}
\value{
Numeric vector. Kernel similarities \code{k} of length \code{length(dose_set)}, with entries in \verb{(0,1]}.
}
\description{
\code{kernel_fun()} computes kernel similarities between a target dose value \code{dose} and a set of dose
locations \code{dose_set} (typically on a standardized scale, e.g., \verb{[0,1]}).

Unlike the conventional parameterization that requires users to specify decay parameters
\code{theta} directly, this kernel is parameterized by \strong{interpretable neighbor kernel values}:
\code{k_left} and \code{k_right}, which represent the desired kernel similarity at a reference distance
\code{ref_gap} on the left and right sides, respectively. The decay parameters are inferred internally via
\deqn{\theta = -\log(k) / \text{ref\_gap}^2.}
}
\details{
The kernel takes the form \eqn{k(d,d_s)=\exp\{-\theta(d-d_s)^2\}}.

\itemize{
\item \strong{Asymmetric mode} (\code{symmetric = FALSE}):
\eqn{\theta_1} is used for dose locations strictly below \code{dose} (\code{dose_set < dose}),
and \eqn{\theta_2} is used for dose locations at or above \code{dose} (\code{dose_set >= dose}),
where
\deqn{\theta_1 = -\log(k_{\text{left}})/\text{ref\_gap}^2,\qquad
          \theta_2 = -\log(k_{\text{right}})/\text{ref\_gap}^2.}
If \code{k_right > k_left}, then \eqn{\theta_2 < \theta_1}, meaning the kernel decays more slowly
on the right side (stronger borrowing from higher doses), matching the typical safety-oriented
SKBD/BKP borrowing direction.

\item \strong{Symmetric mode} (\code{symmetric = TRUE}):
A single \eqn{\theta} is inferred using \code{k_right} (or its default) at distance \code{ref_gap}.
}

The default \code{ref_gap} is set to the minimum adjacent spacing in \code{dose_set}, i.e.,
\code{min(diff(dose_set))}. This makes \code{k_left}/\code{k_right} interpretable as the desired borrowing strength
at the smallest dose spacing on the current grid.
}
\examples{
dose_set = c(0.25, 0.50, 0.75)

# Asymmetric borrowing: weaker from the left (0.2), stronger from the right (0.8)
kernel_fun(dose = 0.50, dose_set = dose_set, symmetric = FALSE, k_left = 0.2, k_right = 0.8)

# Symmetric borrowing using k_right as the reference similarity
kernel_fun(dose = 0.50, dose_set = dose_set, symmetric = TRUE, k_right = 0.5)

# Fix the reference distance explicitly (instead of using min(diff(dose_set)))
kernel_fun(dose = 0.50, dose_set = dose_set, symmetric = FALSE,
           k_left = 0.2, k_right = 0.8, ref_gap = 0.25)

}
