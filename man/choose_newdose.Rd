% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choose_newdose.R
\name{choose_newdose}
\alias{choose_newdose}
\title{Choose an inserted dose within an eligible interval by maximizing target-key probability}
\usage{
choose_newdose(
  dl,
  dr,
  dose_set_work,
  n_dlt_work,
  n_treated_work,
  pri_alpha,
  pri_beta,
  key_L,
  key_U,
  symmetric,
  k_left = 0.2,
  k_right = 0.8,
  ref_gap = NULL,
  M = 100
)
}
\arguments{
\item{dl}{Numeric scalar. Left endpoint of the eligible interval; must satisfy \code{dl < dr}.}

\item{dr}{Numeric scalar. Right endpoint of the eligible interval; must satisfy \code{dl < dr}.}

\item{dose_set_work}{Numeric vector. Current working dose grid (often standardized to \verb{[0, 1]}).}

\item{n_dlt_work}{Numeric/integer vector. Number of DLTs observed at each working dose.
Must have the same length as \code{dose_set_work}.}

\item{n_treated_work}{Numeric/integer vector. Number treated at each working dose.
Must have the same length as \code{dose_set_work}.}

\item{pri_alpha}{Numeric scalar. Prior alpha for the Beta pseudo-posterior.}

\item{pri_beta}{Numeric scalar. Prior beta for the Beta pseudo-posterior.}

\item{key_L}{Numeric scalar. Lower bound of the target key, in \verb{(0,1)}.}

\item{key_U}{Numeric scalar. Upper bound of the target key, in \verb{(0,1)} with \code{key_L < key_U}.}

\item{symmetric}{Logical. Passed to \code{kernel_fun()} to indicate symmetric vs asymmetric kernel mode.}

\item{k_left}{Numeric scalar in \verb{(0,1)}. Passed to \code{kernel_fun()} as left-side
neighbor borrowing strength (used when \code{dose_set < dose}).}

\item{k_right}{Numeric scalar in \verb{(0,1)}. Passed to \code{kernel_fun()} as right-side
neighbor borrowing strength (used when \code{dose_set >= dose}, and as the symmetric
borrowing strength when \code{symmetric = TRUE}).}

\item{ref_gap}{Optional positive scalar. Passed to \code{kernel_fun()} as the reference
spacing used to infer decay from \code{k_left}/\code{k_right}.}

\item{M}{Integer. Number of grid points inside \verb{(dl, dr)} used for search. Default is \code{100}.}
}
\value{
A list with components:
\describe{
\item{newdose}{Selected new dose in \verb{(dl, dr)}. Returns \code{NA_real_} if no valid candidate is found
or if the selected dose is too close to an existing dose.}
\item{q_max}{Maximum value of \code{q(d)} over the search grid (or \code{NA_real_} if not available).}
\item{grid}{The grid of candidate dose values used for searching (returned when available).}
\item{q_grid}{Vector of \code{q(d)} values over \code{grid}, possibly containing \code{NA} for invalid points.}
}
}
\description{
\code{choose_newdose()} selects a new dose level within an eligible open interval \eqn{(d_l, d_r)}
by searching over a grid and maximizing
\deqn{q(d) = \Pr\{ \text{key}_L < \pi(d) \le \text{key}_U \mid \mathcal{D} \}.}
The probability \eqn{q(d)} is computed under the BKP/SKBD pseudo-posterior at candidate dose
locations, where Beta parameters are formed by kernel-weighted pseudo-counts aggregated from
already-observed doses.
}
\details{
\strong{How it works}
\itemize{
\item Collect observed doses \eqn{S=\{j: n_j>0\}} from the current working grid.
\item Build an equally spaced grid of size \code{M} within \eqn{(d_l,d_r)} (excluding endpoints).
\item For each grid point \eqn{d}, compute kernel similarities \code{k(d, dose_obs)}, normalize to
weights \eqn{w}, and obtain Beta pseudo-posterior parameters:
\deqn{\alpha(d)=\alpha_0 + \sum_{j\in S} w_j(d) y_j,\qquad
              \beta(d)=\beta_0 + \sum_{j\in S} w_j(d) (n_j-y_j).}
\item Compute \eqn{q(d)} as \code{pbeta(key_U, alpha, beta) - pbeta(key_L, alpha, beta)}.
\item Return the grid point maximizing \eqn{q(d)}.
}

\strong{Numerical stabilizations}
\itemize{
\item Rescales kernel similarities by \code{max(k)} to reduce underflow when values are extremely small.
\item Guards against zero/invalid normalization and clamps Beta parameters to be at least \code{1e-12}.
\item Avoids duplicating an existing dose by returning \code{NA} if the selected \code{newdose} is within a
small tolerance of any value in \code{dose_set_work}.
}
}
\examples{
# Suppose three doses have been tried and we want to insert between 0.25 and 0.50
dose_set_work = c(0.25, 0.50, 0.75)
n_treated_work = c(3, 6, 0)
n_dlt_work = c(0, 3, 0)

# Candidate selection
out = choose_newdose(
  dl = 0.25, dr = 0.50,
  dose_set_work = dose_set_work,
  n_dlt_work = n_dlt_work,
  n_treated_work = n_treated_work,
  pri_alpha = 0.5, pri_beta = 0.5,
  key_L = 0.25, key_U = 0.35,
  symmetric = FALSE, k_left = 0.2, k_right = 0.8, M = 100
)
out$newdose

}
