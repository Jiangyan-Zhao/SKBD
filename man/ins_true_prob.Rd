% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ins_true_prob.R
\name{ins_true_prob}
\alias{ins_true_prob}
\title{Assign "true" toxicity probability for an inserted dose (simulation-only)}
\usage{
ins_true_prob(
  insert_code,
  d,
  p,
  target,
  LOC_BELOW_MIN = -1L,
  LOC_ABOVE_MAX = -2L
)
}
\arguments{
\item{insert_code}{Integer. Insertion location code:
\code{i >= 1} for inserting between doses \verb{(i, i+1)};
\code{LOC_BELOW_MIN} for inserting below the minimum dose;
\code{LOC_ABOVE_MAX} for inserting above the maximum dose.}

\item{d}{Integer. Current dose index at which insertion is triggered (used for boundary cases).
Must satisfy \verb{1 <= d <= length(p)}.}

\item{p}{Numeric vector. Current \emph{true} toxicity probabilities aligned with the working dose grid
(simulation-only). Must have values in \verb{[0, 1]}.}

\item{target}{Numeric scalar. Target toxicity rate \eqn{\phi} in \verb{(0, 1)}.}

\item{LOC_BELOW_MIN}{Integer. Code representing insertion below minimum dose. Default \code{-1L}.}

\item{LOC_ABOVE_MAX}{Integer. Code representing insertion above maximum dose. Default \code{-2L}.}
}
\value{
Numeric scalar. The assigned true toxicity probability for the inserted dose, clamped to
\verb{(\eqn{\epsilon}, 1-\eqn{\epsilon})} with \verb{\eqn{\epsilon}=1e-6}.
}
\description{
\code{ins_true_prob()} is a \strong{simulation-only} helper used in operating-characteristics (OC)
evaluation of adaptive dose insertion. Given the current \emph{true} dose-toxicity vector \code{p}
on the working grid, it assigns a "true toxicity probability" to the newly inserted dose
according to simple ADM-style rules.

In a real trial, the true toxicity probabilities are unknown, so this function is \strong{not}
used in practice.
}
\details{
The function supports three insertion locations encoded by \code{insert_code}:
\itemize{
\item \strong{Between} \eqn{(i, i+1)}: \code{insert_code = i} with \code{i >= 1}.
If \eqn{p_i < \phi < p_{i+1}}, set \eqn{p_{\text{new}} = \phi}; otherwise set
\eqn{p_{\text{new}} = (p_i + p_{i+1})/2}.
\item \strong{Below minimum}: \code{insert_code = LOC_BELOW_MIN} (default \code{-1L}).
If \eqn{\phi < p_d}, set \eqn{p_{\text{new}} = \phi}; otherwise set \eqn{p_{\text{new}} = p_d/2}.
\item \strong{Above maximum}: \code{insert_code = LOC_ABOVE_MAX} (default \code{-2L}).
If \eqn{\phi > p_d}, set \eqn{p_{\text{new}} = \phi}; otherwise set \eqn{p_{\text{new}} = p_d + 0.1}.
}

For numerical stability in simulations, the returned probability is clamped to
\eqn{(\epsilon, 1-\epsilon)} with \eqn{\epsilon = 10^{-6}}.
}
\examples{
# Between (2, 3)
p = c(0.05, 0.15, 0.30, 0.45)
ins_true_prob(insert_code = 2L, d = 2L, p = p, target = 0.25)

# Below minimum
ins_true_prob(insert_code = -1L, d = 1L, p = p, target = 0.25)

# Above maximum
ins_true_prob(insert_code = -2L, d = length(p), p = p, target = 0.25)

}
