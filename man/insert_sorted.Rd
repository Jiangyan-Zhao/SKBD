% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insert_sorted.R
\name{insert_sorted}
\alias{insert_sorted}
\title{Insert a new dose into the working grid and keep all trial vectors aligned}
\usage{
insert_sorted(
  dose_set_work,
  tox_prob_work,
  n_dlt_work,
  n_treated_work,
  is_eliminated,
  newdose,
  tox_new
)
}
\arguments{
\item{dose_set_work}{Numeric vector. Current working dose grid (typically standardized to \verb{[0, 1]}).
Must be strictly increasing and aligned with all other vectors.}

\item{tox_prob_work}{Numeric vector. Current true toxicity probabilities aligned with \code{dose_set_work}
(simulation-only). Must have the same length as \code{dose_set_work}.}

\item{n_dlt_work}{Integer/numeric vector. Current number of observed DLTs aligned with \code{dose_set_work}.
Must have the same length as \code{dose_set_work}.}

\item{n_treated_work}{Integer/numeric vector. Current number treated aligned with \code{dose_set_work}.
Must have the same length as \code{dose_set_work}.}

\item{is_eliminated}{Logical vector. Dose elimination indicators aligned with \code{dose_set_work}.
Must have the same length as \code{dose_set_work}.}

\item{newdose}{Numeric scalar. The newly inserted dose value.}

\item{tox_new}{Numeric scalar. The (simulation-only) true toxicity probability at \code{newdose}.}
}
\value{
A list with components:
\describe{
\item{dose_set_work}{Sorted working dose grid after insertion.}
\item{tox_prob_work}{Reordered toxicity probabilities aligned with the new grid.}
\item{n_dlt_work}{Reordered DLT counts aligned with the new grid (0 at the inserted dose).}
\item{n_treated_work}{Reordered treated counts aligned with the new grid (0 at the inserted dose).}
\item{is_eliminated}{Reordered elimination indicators aligned with the new grid (FALSE at the inserted dose).}
\item{d_idx}{Integer index of \code{newdose} in the sorted \code{dose_set_work}.}
}
}
\description{
\code{insert_sorted()} inserts a new dose value (\code{newdose}) into the current working
dose grid (\code{dose_set_work}), then \strong{sorts} the updated grid and \strong{reorders}
all associated vectors accordingly:
\itemize{
\item \code{tox_prob_work}: (simulation-only) true toxicity probabilities aligned with doses
\item \code{n_dlt_work}: number of DLTs observed at each dose
\item \code{n_treated_work}: number treated at each dose
\item \code{is_eliminated}: dose elimination indicators
}
It returns the updated vectors and the index of the inserted dose in the
sorted grid (\code{d_idx}).
}
\details{
This helper is designed for adaptive dose insertion routines (e.g., ADM/SKBD
insertion) where the dose grid can expand during the trial. By centralizing
the "insert + sort + reorder" operation, it prevents common bugs such as
mismatched vector lengths, inconsistent ordering across vectors, or incorrect
dose indexing after insertion.

\strong{Important:} This function does not check whether \code{newdose} already exists
in \code{dose_set_work}. Callers should guard against duplicates (e.g., using a
tolerance check) before calling \code{insert_sorted()}.
}
\examples{
dose_set_work = c(0.25, 0.50, 0.75)
tox_prob_work = c(0.10, 0.25, 0.40)
n_dlt_work = c(0, 1, 2)
n_treated_work = c(3, 6, 6)
is_eliminated = c(FALSE, FALSE, FALSE)

newdose = 0.375
tox_new = 0.20

out = insert_sorted(
  dose_set_work, tox_prob_work, n_dlt_work, n_treated_work,
  is_eliminated, newdose, tox_new
)
out$dose_set_work
out$d_idx

}
